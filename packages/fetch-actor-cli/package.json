{
  "name": "@actor-types/fetch-actor-cli",
  "version": "1.0.4",
  "description": "CLI tool to fetch and generate actor types",
  "bin": {
    "fetch-actor": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist",
    "start": "node dist/cli.js",
    "dev": "ts-node src/cli.ts"
  },
  "dependencies": {
    "@actor-types/generator": "workspace:^1.0.0",
    "apify": "^3.0.0",
    "apify-client": "^2.12.2",
    "commander": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.1.1",
    "eslint": "^8.0.0",
    "ts-node": "^10.9.1",
    "@types/commander": "^2.12.2"
  }
}
