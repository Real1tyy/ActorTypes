{
  "name": "@actor-types/input-mapper-cli",
  "version": "1.0.0",
  "description": "CLI tool to generate input types from local schema",
  "bin": {
    "generate-input": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist",
    "start": "node dist/cli.js",
    "dev": "ts-node src/cli.ts"
  },
  "dependencies": {
    "@actor-types/generator": "^1.0.0",
    "commander": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "eslint": "^8.0.0",
    "ts-node": "^10.9.1",
    "@types/commander": "^2.12.2"
  }
}
